import styled from "styled-components";

export const TempProperty = styled.div`
    display: flex;
    align-items: center;
    column-gap: 6px;

    p {
        font-weight: 400;
        font-size: 22px;
        color: inherit;
    }
`;